# Third-party references

`VSSD` is pinned as a Git submodule for architectural reference and provenance.
It is not imported by the `vnct` package and its legacy dependencies are not
installed into the VNCT environment.
